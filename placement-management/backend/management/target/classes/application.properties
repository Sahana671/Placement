spring.application.name=management

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://gateway01.ap-northeast-1.prod.aws.tidbcloud.com:4000/test?useSSL=true&verifyServerCertificate=false}
spring.datasource.username=${DB_USERNAME:3pD3WVby13diBaj.root}
spring.datasource.password=${DB_PASSWORD:W3Yv35WRwhK5cbY7}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Custom Global Variable Object for API Endpoints & CORS Configuration
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5173,https://*.vercel.app}
app.api.base-path=/api/v1
app.api.endpoints.students=/api/v1/students
app.api.endpoints.companies=/api/v1/companies